2-D Arrays

 - Assigning rows and columns to Arrays.
 - It is Array's of Arrays.
 - It is in (n x m) format (rows X columns).
   [00 , 01 , 02]
   [10 , 11 , 12]
   [20 , 21 , 22] n x m

 - int [] [] arr = new int [rows.size][columns.size].
 - We will iterate first row then column.

 -Stored in memory by
   - Row major
   - Column major
   - But in java it creates different arrays and stored randomly.


 - Pictures can be stored in 3-D (RGB).
   Video can be store in 4-D , 4th dimension is time.



    public static void printMatrices(int [][] nums){
    // int [][]nums = new int [3][3];        (in case we want to build a matrix)
    int n = nums.length;
    int m = nums[0].length;                 // nums[0] =  means 0th index of nums's length

    for(int i=0 ;i<n;i++){
       for(int j=0 ; j<m;j++){
       System.out.print(nums[i][j]);
       }
    }

    }